import React  from 'react'
import  Quill from 'quill'

class OpCKEditor extends React.Component{
constructor(props){
    super(props)
    this.state = {
        editorhtml : '',
        theme:'snow'
    }
    this.handleChange = this.handleChange.bind(this)
    }

    handleChange 
    
 }
}

export default OpCKEditor
