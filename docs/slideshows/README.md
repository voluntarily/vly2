# Documentation Slideshows.

Each slideshow is in a markdown file. Each slide is separated by --- going horizontally and ---- going vertically.

To run:

    npm install -g reveal-md
    reveal-md docs/slideshows/developer_overview.md -w
