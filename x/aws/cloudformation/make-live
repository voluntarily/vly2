aws cloudformation deploy \
  --capabilities CAPABILITY_NAMED_IAM \
  --template-file fargate.yml \
  --stack-name vly-live \
  --parameter-overrides ServiceName=vly-live AppUrl=https://live.voluntarily.nz
